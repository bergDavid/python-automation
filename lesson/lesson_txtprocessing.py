#-------------------------------------------------------------------
# Networking: text processing
# Unicode Char UTF-8 is the recomended for encoding exchange data
#-------------------------------------------------------------------
# Check the order of the char
# print(ord('\n'))
x = b'abc'
print(type(x))
